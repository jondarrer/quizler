/**
 *
 * @param {Array<string>} options
 * @param {Array<string>} answers
 * @returns number The number of options found in answers
 */
const contains = (options, answers) => 0;

module.exports = contains;
